# Spring_Course
## Веб-додаток для генерації меню страв
### Functional Requirements:
1. Як користувач, я хочу мати можливість створити обліковий запис, щоб мати доступ до функціоналу веб-застосунку.
2. Як користувач, я хочу мати можливість авторизуватися у системі, щоб мати доступ до персоналізованого контенту.
3. Як користувач, я хочу мати можливість згенерувати меню на вказаний проміжок часу (від одного дня до одного місяця), щоб планувати свої прийоми їжі.
4. Як користувач, я хочу мати можливість видалити одну або декілька страв з меню, щоб налаштовувати свій список страв.
5. Як користувач, я хочу мати можливість замінити одну або декілька обраних страв з меню на випадкові інші страви, щоб урізноманітнити своє харчування.
6. Як користувач, я хочу мати можливість сортувати страви за категоріями (сніданок, обід, вечеря), щоб зручно переглядати доступні варіанти.
7. Як користувач, я хочу мати можливість вказати продукт, якого не буде в жодній згенерованій страві, та зберегти цей вибір для майбутніх генерацій меню.
8. Як користувач, я хочу мати можливість зберегти одну або декілька страв до розділу "Улюблені", щоб швидко знаходити їх у майбутньому.
9. Як користувач, я хочу мати можливість зберегти ціле меню до розділу "Улюблені", щоб повторно використовувати його у майбутньому.
10. Як користувач, я хочу, щоб меню складалося з трьох страв (сніданок, обід, вечеря), щоб мати різноманітні прийоми їжі протягом дня.
11. Як користувач, я хочу, щоб кожна страва містила опис, який включає назву страви, зображення, інгредієнти та, якщо можливо, рецепт і покрокову інструкцію для приготування, щоб мати повну інформацію про страви.
12. Як адміністратор, я хочу мати можливість додавати нові страви до списку доступних для генерації меню, щоб розширити асортимент для користувачів.
13. Як адміністратор, я хочу мати можливість редагувати існуючі страви (назву, зображення, інгредієнти, рецепт тощо), щоб оновлювати інформацію і виправляти помилки.
14. Як адміністратор, я хочу мати можливість видаляти страви зі списку доступних, якщо вони більше не актуальні або більше не пропонуються.

### System behavior:
1. **Створення облікового запису:**
   - При введенні користувачем необхідної інформації (електронної пошти та пароля), система створює новий обліковий запис.
   - Система перевіряє унікальність електронної пошти для уникнення дублікатів облікових записів.

2. **Авторизація користувача:**
   - При введенні вірної електронної пошти та пароля, система надає користувачу доступ до особистого контенту.
   - У разі невірного введення даних система відображає повідомлення про помилку.

3. **Генерація меню на вказаний проміжок часу:**
   - Користувач обирає проміжок часу для генерації меню (наприклад, день, тиждень, місяць).
   - Система автоматично створює меню, враховуючи вказаний проміжок часу та інші вимоги.
  
4. **Можливість видалення страв з меню:**
   - Користувач може обирати одну або декілька страв для видалення зі свого згенерованого меню.
   - Після підтвердження видалення, система вилучає вибрані страви з меню користувача та заміняє їх іншими.

5. **Заміна обраних страв з меню на випадкові інші страви:**
   - Користувач може обирати одну або декілька страв для заміни на випадкові інші.
   - Система автоматично замінює вибрані страви новими випадковими варіантами з доступного асортименту.

6. **Збереження страв до розділу "Улюблені":**
   - Користувач може вибрати страву та зберегти її до розділу "Улюблені" для майбутнього швидкого доступу.
   - Система забезпечує можливість перегляду та управління списком улюблених страв користувача.

7. **Збереження цілого меню до розділу "Улюблені":**
   - Користувач може зберегти згенероване меню (складене з трьох страв на день) до розділу "Улюблені".
   - Система забезпечує можливість перегляду та управління списком улюблених меню користувача.

8. **Управління списоком страв:**
   - Адміністратор може створювати, редагувати та видаляти страви.
   - Система надає зручний інтерфейс для створення, редагування та видалення страв.

9. **Подивитися список страв:**
   - Адміністратор може переглянути повний список страв, які доступні для генерації меню.
   - Система надає зручний інтерфейс для перегляду усіх страв, їх опису та зображень.
   
### REST API endpoints:

### User Endpoints:

1. **Отримати всіх користувачів (```GET /api/users```)**
   - Опис: Повертає список усіх користувачів.
   - Метод: GET
   - Відповідь: Список користувачів у форматі JSON

2. **Отримати користувача за ідентифікатором (```GET /api/users/{id}```)**
   - Опис: Повертає користувача за його ідентифікатором.
   - Метод: GET
   - Параметри: id - ідентифікатор користувача
   - Відповідь: Об'єкт користувача у форматі JSON або повідомлення про помилку

3. **Створити нового користувача (```POST /api/users```)**
   - Опис: Створює нового користувача з наданими даними.
   - Метод: POST
   - Параметри: Об'єкт користувача у форматі JSON
   - Відповідь: Створений користувач у форматі JSON або повідомлення про помилку

4. **Оновити існуючого користувача (```PUT /api/users/{id}```)**
   - Опис: Оновлює існуючого користувача за його ідентифікатором з новими даними.
   - Метод: PUT
   - Параметри: id - ідентифікатор користувача, Об'єкт користувача у форматі JSON
   - Відповідь: Оновлений користувач у форматі JSON або повідомлення про помилку

5. **Видалити користувача (```DELETE /api/users/{id}```)**
   - Опис: Видаляє користувача за його ідентифікатором.
   - Метод: DELETE
   - Параметри: id - ідентифікатор користувача
   - Відповідь: Порожній вміст з кодом статусу 204, якщо користувач успішно видалений, або повідомлення про помилку

### Dish Endpoints:

1. **Отримати всі страви (```GET /api/dishes```)**
   - Опис: Повертає список усіх доступних страв.
   - Метод: GET
   - Відповідь: Список страв у форматі JSON

2. **Отримати конкретну страву за ідентифікатором (```GET /api/dishes/{id}```)**
   - Опис: Повертає конкретну страву за її ідентифікатором.
   - Метод: GET
   - Параметри: id - ідентифікатор страви
   - Відповідь: Об'єкт страви у форматі JSON або повідомлення про помилку, якщо страва з вказаним ідентифікатором не знайдена

3. **Створити нову страву (```POST /api/dishes```)**
   - Опис: Створює нову страву з наданими даними.
   - Метод: POST
   - Параметри: Об'єкт страви у форматі JSON
   - Відповідь: Створена страва у форматі JSON або повідомлення про помилку, якщо страву не вдалося створити

4. **Оновити існуючу страву (```PUT /api/dishes/{id}```)**
   - Опис: Оновлює існуючу страву за її ідентифікатором з новими даними.
   - Метод: PUT
   - Параметри: id - ідентифікатор страви, Об'єкт страви у форматі JSON
   - Відповідь: Оновлена страва у форматі JSON або повідомлення про помилку, якщо страву не вдалося оновити

5. **Видалити страву (```DELETE /api/dishes/{id}```)**
   - Опис: Видаляє страву за її ідентифікатором.
   - Метод: DELETE
   - Параметри: id - ідентифікатор страви
   - Відповідь: Порожній вміст з кодом статусу 204, якщо страва успішно видалена, або повідомлення про помилку, якщо страву не вдалося видалити.

### Menu Endpoints:

1. **Отримати всі меню (```GET /api/menus```)**
   - Опис: Повертає список усіх доступних меню.
   - Метод: GET
   - Відповідь: Список меню у форматі JSON

2. **Отримати меню за ідентифікатором (```GET /api/menus/{id}```)**
   - Опис: Повертає меню за його ідентифікатором.
   - Метод: GET
   - Параметри: id - ідентифікатор меню
   - Відповідь: Об'єкт меню у форматі JSON або повідомлення про помилку

3. **Створити нове меню (```POST /api/menus```)**
   - Опис: Створює нове меню з наданими даними.
   - Метод: POST
   - Параметри: Об'єкт меню у форматі JSON
   - Відповідь: Створене меню у форматі JSON або повідомлення про помилку

4. **Оновити існуюче меню (```PUT /api/menus/{id}```)**
   - Опис: Оновлює існуюче меню за його ідентифікатором з новими даними.
   - Метод: PUT
   - Параметри: id - ідентифікатор меню, Об'єкт меню у форматі JSON
   - Відповідь: Оновлене меню у форматі JSON або повідомлення про помилку

5. **Видалити меню (```DELETE /api/menus/{id}```)**
   - Опис: Видаляє меню за його ідентифікатором.
   - Метод: DELETE
   - Параметри: id - ідентифікатор меню
   - Відповідь: Порожній вміст з кодом статусу 204, якщо меню успішно видалене, або повідомлення про помилку

### Favourites Endpoints:

1. **Отримати всі улюблені страви користувача (```GET /api/favourites/{user_id}```)**
   - Опис: Повертає список усіх улюблених страв для вказаного користувача.
   - Метод: GET
   - Параметри: user_id - ідентифікатор користувача
   - Відповідь: Список улюблених страв у форматі JSON

2. **Додати страву до улюблених (```POST /api/favourites```)**
   - Опис: Додає вказану страву до улюблених страв користувача.
   - Метод: POST
   - Параметри: Об'єкт з ідентифікаторами користувача та страви у форматі JSON
   - Відповідь: Повідомлення про успішне додавання або повідомлення про помилку

3. **Видалити страву з улюблених (```DELETE /api/favourites```)**
   - Опис: Видаляє вказану страву зі списку улюблених страв користувача.
   - Метод: DELETE
   - Параметри: Об'єкт з ідентифікаторами користувача та страви у форматі JSON
   - Відповідь: Повідомлення про успішне видалення або повідомлення про помилку
